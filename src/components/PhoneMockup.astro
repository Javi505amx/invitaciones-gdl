<div class="relative animate-float">
  <!-- Phone frame -->
  <div class="w-72 h-[560px] bg-gradient-to-b from-gray-900 to-gray-800 rounded-[40px] p-5 shadow-2xl border border-gray-700">
    <!-- Camera notch -->
    <div class="absolute top-3 left-1/2 transform -translate-x-1/2 w-24 h-3 bg-gray-800 rounded-full"></div>
    
    <!-- Screen -->
    <div class="w-full h-full bg-gradient-to-br from-stone-50 to-gray-100 rounded-[32px] overflow-hidden relative">
      <!-- Invitation preview -->
      <div class="absolute inset-0 p-6 flex flex-col items-center justify-center">
        <!-- Elegant monogram -->
        <div class="w-20 h-20 bg-gradient-to-br from-gray-200 to-gray-100 rounded-full flex items-center justify-center mb-6 shadow-inner border border-gray-300">
          <span class="text-2xl font-serif text-gray-800">M&C</span>
        </div>
        
        <h3 class="text-2xl font-light text-gray-800 mb-2 text-center">María & Carlos</h3>
        <p class="text-base text-gray-600 font-light mb-6 tracking-wide">Boda de Lujo</p>
        
        <div class="bg-white rounded-lg p-4 shadow-sm mb-6 w-full max-w-xs border border-gray-200">
          <p class="text-gray-800 font-light mb-1 text-sm">Sábado, 15 de Junio de 2024</p>
          <p class="text-gray-600 text-sm">Iglesia Santa María</p>
        </div>
        
        <button class="bg-gradient-to-r from-gray-800 to-black text-white px-6 py-2.5 rounded-lg font-light shadow-md hover:shadow-lg transition-all duration-300 transform hover:scale-105 text-sm uppercase tracking-wider">
          Confirmar Asistencia
        </button>
        
        <!-- Decorative elements -->
        <div class="absolute top-10 right-6 w-2 h-2 rounded-full bg-gray-400 animate-pulse"></div>
        <div class="absolute bottom-20 left-6 w-2 h-2 rounded-full bg-gray-400 animate-pulse"></div>
      </div>
    </div>
  </div>
  
  <!-- Floating animation -->
  <style>
    .animate-float {
      animation: float 6s ease-in-out infinite;
    }
    
    @keyframes float {
      0% { transform: translateY(0px); }
      50% { transform: translateY(-20px); }
      100% { transform: translateY(0px); }
    }
  </style>
</div>